<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project_s">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/arirang"
        android:label="@string/app_name"
        android:roundIcon="@drawable/arirang"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_S"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".StockActivity" />
        <activity android:name=".WebView" />
        <activity android:name=".NewsWebView" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".Category" />
        <activity android:name=".LoginActivity"/>
        <activity android:name=".ModeMainActivity"/>
        <activity android:name=".ModeNewsActivity"/>
        <activity android:name=".ModeCategory"/>
        <activity android:name=".ModeStockActivity"/>
    </application>

</manifest>